<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <appender name="console" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>
                %d{yyyy-MM-dd HH:mm:ss.SSS} | [Gateway,%X{traceId},%X{spanId}] [%X{session},%X{user_id}] | %-5level | %logger{36} | %msg%n
            </pattern>
        </encoder>
    </appender>
    <logger name="org.springframework.security.web.server.savedrequest" level="DEBUG">
        <appender-ref ref="console"/>
    </logger>
    <logger name="org.springframework.security.web.server.authentication.AuthenticationWebFilter" level="DEBUG">
        <appender-ref ref="console"/>
    </logger>
    <root level="INFO">
        <appender-ref ref="console"/>
    </root>
</configuration>